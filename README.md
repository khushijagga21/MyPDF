# MyPDF

Premium PDF tools — merge, split, compress, and convert PDFs in your browser.

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Environment

Copy `.env.example` to `.env` and set `DATABASE_URL` and `AUTH_SECRET`.
